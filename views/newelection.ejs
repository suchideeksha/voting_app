<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
</head>
    <b>
        <a href="/elections"><span class="text-white bg-red-600 hover:bg-red-800 font-medium rounded px-3 py-1">PREVIOUS PAGE</a></span>
    </b>

    <body style="background-color: rgba(78, 152, 118, 0.873);;">
        <div class="grid grid-cols-6">
            <div class="col-start-3 col-span-2">
                <span class="text-2xl font-dark ">CREATE NEW ELECTION</span>
              <form action="/elections" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="py-3">
               <label for="electionname">ENETER ELECTION NAME :</label>
               <input id="electionName" name="electionName" type="text" placeholder="Election name" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
            </div>  
            <div class="py-3"> 
               <label for="publicurl">ENETER PUBLIC URL:</label>
               <input id="publicurl" name="publicurl" type="text" placeholder="URL" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
            </div>  
            <div class="py-3"> 
               <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">CREATE ELECTION</button>        
            </div>  
            </form>
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                  <p class="bg-red-200 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
                <% } %>
              <% } %>
            </div>
        </div>
      </body>
</html>
